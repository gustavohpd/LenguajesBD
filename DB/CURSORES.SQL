SET SERVEROUTPUT ON;

--ESTADOS
DECLARE
    CURSOR C_ESTADOS IS
        SELECT ESTADO_ID, NOMBRE_ESTADO
        FROM FIDE_ESTADOS_TB;
BEGIN
    FOR I IN C_ESTADOS LOOP
        DBMS_OUTPUT.PUT_LINE('ID: ' || I.ESTADO_ID || ' - ' || I.NOMBRE_ESTADO);
    END LOOP;

EXCEPTION
    WHEN NO_DATA_FOUND THEN
        DBMS_OUTPUT.PUT_LINE('No hay estados registrados.');
    WHEN OTHERS THEN
        DBMS_OUTPUT.PUT_LINE('ERROR ESTADOS: ' || SQLCODE || ' - ' || SQLERRM);
END;
/


--ROLES
DECLARE
    CURSOR C_ROLES IS
        SELECT ROL_ID, NOMBRE_ROL, ESTADO_ID
        FROM FIDE_ROLES_TB;
BEGIN
    FOR I IN C_ROLES LOOP
        IF I.ESTADO_ID = 1 THEN
            DBMS_OUTPUT.PUT_LINE(I.NOMBRE_ROL || ' - ACTIVO');
        ELSE
            DBMS_OUTPUT.PUT_LINE(I.NOMBRE_ROL || ' - INACTIVO');
        END IF;
    END LOOP;

EXCEPTION
    WHEN NO_DATA_FOUND THEN
        DBMS_OUTPUT.PUT_LINE('No hay roles.');
    WHEN OTHERS THEN
        DBMS_OUTPUT.PUT_LINE('ERROR ROLES: ' || SQLCODE || ' - ' || SQLERRM);
END;
/


--CORREOS
DECLARE
    CURSOR C_CORREOS IS
        SELECT CORREO_ID, CORREO, ESTADO_ID
        FROM FIDE_CORREOS_TB;
BEGIN
    FOR I IN C_CORREOS LOOP
        DBMS_OUTPUT.PUT_LINE('Correo: ' || I.CORREO);
    END LOOP;

EXCEPTION
    WHEN NO_DATA_FOUND THEN
        DBMS_OUTPUT.PUT_LINE('No hay correos.');
    WHEN OTHERS THEN
        DBMS_OUTPUT.PUT_LINE('ERROR CORREOS: ' || SQLCODE || ' - ' || SQLERRM);
END;
/


--TELEFONOS
DECLARE
    CURSOR C_TELEFONOS IS
        SELECT TELEFONO_ID, TELEFONO, ESTADO_ID
        FROM FIDE_TELEFONOS_TB;
BEGIN
    FOR I IN C_TELEFONOS LOOP
        DBMS_OUTPUT.PUT_LINE('Teléfono: ' || I.TELEFONO);
    END LOOP;

EXCEPTION
    WHEN NO_DATA_FOUND THEN
        DBMS_OUTPUT.PUT_LINE('No hay teléfonos.');
    WHEN OTHERS THEN
        DBMS_OUTPUT.PUT_LINE('ERROR TELÉFONOS: ' || SQLCODE || ' - ' || SQLERRM);
END;
/


--USUARIOS
DECLARE
    CURSOR C_USUARIOS IS
        SELECT USUARIO_ID, NOMBRE, APELLIDO_PATERNO, ESTADO_ID
        FROM FIDE_USUARIOS_TB;
BEGIN
    FOR I IN C_USUARIOS LOOP
        IF I.ESTADO_ID = 1 THEN
            DBMS_OUTPUT.PUT_LINE(I.NOMBRE || ' ' || I.APELLIDO_PATERNO || ' - ACTIVO');
        END IF;
    END LOOP;

EXCEPTION
    WHEN NO_DATA_FOUND THEN
        DBMS_OUTPUT.PUT_LINE('No hay usuarios.');
    WHEN OTHERS THEN
        DBMS_OUTPUT.PUT_LINE('ERROR USUARIOS: ' || SQLCODE || ' - ' || SQLERRM);
END;
/


--CLIENTES
DECLARE
    CURSOR C_CLIENTES IS
        SELECT CLIENTE_ID, USUARIO_ID, ESTADO_ID
        FROM FIDE_CLIENTES_TB;
BEGIN
    FOR I IN C_CLIENTES LOOP
        DBMS_OUTPUT.PUT_LINE(
            'Cliente ID: ' || I.CLIENTE_ID ||
            ' | Usuario: ' || I.USUARIO_ID
        );
    END LOOP;

EXCEPTION
    WHEN NO_DATA_FOUND THEN
        DBMS_OUTPUT.PUT_LINE('No hay clientes registrados.');
    WHEN OTHERS THEN
        DBMS_OUTPUT.PUT_LINE('ERROR CLIENTES: ' || SQLCODE || ' - ' || SQLERRM);
END;
/


--PROVINCIAS
DECLARE
    CURSOR C_PROVINCIAS IS
        SELECT PROVINCIA_ID, NOMBRE_PROVINCIA, ESTADO_ID
        FROM FIDE_PROVINCIAS_TB;
BEGIN
    FOR I IN C_PROVINCIAS LOOP
        DBMS_OUTPUT.PUT_LINE('Provincia: ' || I.NOMBRE_PROVINCIA);
    END LOOP;

EXCEPTION
    WHEN NO_DATA_FOUND THEN
        DBMS_OUTPUT.PUT_LINE('No hay provincias.');
    WHEN OTHERS THEN
        DBMS_OUTPUT.PUT_LINE('ERROR PROVINCIAS: ' || SQLCODE || ' - ' || SQLERRM);
END;
/


--CANTONES
DECLARE
    CURSOR C_CANTONES IS
        SELECT CANTON_ID, NOMBRE_CANTON, ESTADO_ID
        FROM FIDE_CANTONES_TB;
BEGIN
    FOR I IN C_CANTONES LOOP
        DBMS_OUTPUT.PUT_LINE('Cantón: ' || I.NOMBRE_CANTON);
    END LOOP;

EXCEPTION
    WHEN NO_DATA_FOUND THEN
        DBMS_OUTPUT.PUT_LINE('No hay cantones.');
    WHEN OTHERS THEN
        DBMS_OUTPUT.PUT_LINE('ERROR CANTONES: ' || SQLCODE || ' - ' || SQLERRM);
END;
/


--DISTRITOS
DECLARE
    CURSOR C_DISTRITOS IS
        SELECT DISTRITO_ID, NOMBRE_DISTRITO
        FROM FIDE_DISTRITOS_TB;
BEGIN
    FOR I IN C_DISTRITOS LOOP
        DBMS_OUTPUT.PUT_LINE('Distrito: ' || I.NOMBRE_DISTRITO);
    END LOOP;

EXCEPTION
    WHEN NO_DATA_FOUND THEN
        DBMS_OUTPUT.PUT_LINE('No hay distritos.');
    WHEN OTHERS THEN
        DBMS_OUTPUT.PUT_LINE('ERROR DISTRITOS: ' || SQLCODE || ' - ' || SQLERRM);
END;
/


--SERVICIOS
DECLARE
    CURSOR C_SERVICIOS IS
        SELECT SERVICIO_ID, NOMBRE, PRECIO, ESTADO_ID
        FROM FIDE_SERVICIOS_TB;
BEGIN
    FOR I IN C_SERVICIOS LOOP
        DBMS_OUTPUT.PUT_LINE(I.NOMBRE || ' - ₡' || I.PRECIO);
    END LOOP;

EXCEPTION
    WHEN NO_DATA_FOUND THEN
        DBMS_OUTPUT.PUT_LINE('No hay servicios.');
    WHEN OTHERS THEN
        DBMS_OUTPUT.PUT_LINE('ERROR SERVICIOS: ' || SQLCODE || ' - ' || SQLERRM);
END;
/


--PRODUCTOS
DECLARE
    CURSOR C_PRODUCTOS IS
        SELECT PRODUCTO_ID, NOMBRE, PRECIO, ESTADO_ID
        FROM FIDE_PRODUCTOS_TB;
BEGIN
    FOR I IN C_PRODUCTOS LOOP
        DBMS_OUTPUT.PUT_LINE('Producto: ' || I.NOMBRE || ' - ₡' || I.PRECIO);
    END LOOP;

EXCEPTION
    WHEN NO_DATA_FOUND THEN
        DBMS_OUTPUT.PUT_LINE('No hay productos.');
    WHEN OTHERS THEN
        DBMS_OUTPUT.PUT_LINE('ERROR PRODUCTOS: ' || SQLCODE || ' - ' || SQLERRM);
END;
/


--INVENTARIO
DECLARE
    CURSOR C_INVENTARIO IS
        SELECT INVENTARIO_ID, PRODUCTO_ID, CANTIDAD, FECHA_ACTUALIZACION
        FROM FIDE_INVENTARIO_TB;
BEGIN
    FOR I IN C_INVENTARIO LOOP
        DBMS_OUTPUT.PUT_LINE(
            'Producto ' || I.PRODUCTO_ID ||
            ' | Stock: ' || I.CANTIDAD
        );
    END LOOP;

EXCEPTION
    WHEN NO_DATA_FOUND THEN
        DBMS_OUTPUT.PUT_LINE('No hay inventario registrado.');
    WHEN OTHERS THEN
        DBMS_OUTPUT.PUT_LINE('ERROR INVENTARIO: ' || SQLCODE || ' - ' || SQLERRM);
END;
/

